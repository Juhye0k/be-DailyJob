###### 1. 오늘 완료한 것
- 오늘 실제로 끝낸 기능/수정/학습: HttpRequest, HttpResponse, HttpRequestReader 및 Writer 분리, 단위테스트 작성, dto record 변경
- (오늘의 목표 중에서) 아직 끝내지 못한 부분: 상태코드 enum 관리

###### 2. 오늘 가장 의미있었던 기술적 판단
- 내가 내린 판단: Request, Response 분리와 Reader,Writer의 분리
- 그렇게 판단한 이유: 기존에는 파싱 시 변수를 정의하여 각각 관리하는 방식을 하였다. 그러나 HTTP request line과 헤더, 본문 등 자료가 많기 때문에 처리 메서드 안에서 파싱하면 코드가 매우 길어질 것이라고 판단했다. 따라서 요청, 응답별로 객체를 만들어 필드를 내부에서 관리하는
  방식을 생각했다. 객체와 파서를 분리한 이유는 단일 책임 원칙을 지키기 위해 분리하였다. 객체 자체에 해당하는 것과 또 객체를 파싱하는 부분을 분리해서 관리하는 것이 유지보수가 용이하다고 판단했다.
- 고려했지만 선택하지 않은 대안: 객체 내부에 파서 메서드를 추가할까 생각했지만 코드가 길어질 것이라고 생각했다.
- 이 판단이 코드 품질/테스트/유지보수에 미친 영향: 관심사 필드를 한 객체에 모아서 관리하기 용이하다

###### 3. 가장 막혔던 문제와 해결 과정
- 문제 상황 또는 증상: 200 성공 상태, 302 리다이렉트 상태마다 필요한 헤더가 다른데 어떻게 처리해야할까?
- 해결 방법: 객체 내부에 별도로 생성하는 방식으로 해결했다.

###### 4. 오늘 잘한 행동(개발과 관련된 것)
- 어떤 행동이었는지: AI에게 내가 모르는 부분을 프롬프트로 정확하게 물어보았고, 내가 필요한 값을 정상적으로 받을 수 있었다.
- 그 접근이 도움이 된 이유: 내가 모르는 부분을 정확히 알고 ai에게 질의하여 한 번에 원하는 응답을 받을 수 있었다.
- 다음에도 반복하고 싶은 점: 모르는 것이 생겼을 때 먼저 내가 모르는 부분을 정리하고 물어보기

###### 5. 오늘 아쉬웠던 행동(개발과 관련된 것)
- 비효율적이었던 접근/놓치거나 부족했던 점: 요청,응답 헤더에 필요한 필드를 알지 못했다
- 그렇게 된 원인: 기존에는 프레임워크가 전부 처리해줬기 때문에 신경쓰지 못했다
- 다음에는 다르게 해볼 구체적인 행동: 공식 문서에서 스펙을 확인하고 개발하기
